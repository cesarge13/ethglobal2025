{
  "name": "agricultural-reputation-contracts",
  "version": "1.0.0",
  "description": "Smart Contracts para Sistema de Reputación Agrícola en Polygon Mainnet",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "verify-setup": "tsx ../scripts/verify-deploy-setup.ts",
    "deploy:polygon": "hardhat run ../scripts/deploy-contract.ts --network polygon",
    "verify": "hardhat verify --network polygon"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@types/node": "^20.10.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0",
    "ts-node": "^10.9.1",
    "tsx": "^4.20.6",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "ethers": "^6.8.0"
  }
}
