{
  "name": "agricultural-reputation-scripts",
  "version": "1.0.0",
  "description": "Scripts para despliegue y gestión del sistema de reputación agrícola",
  "scripts": {
    "deploy:polygon": "hardhat run deploy-contract.ts --network polygon",
    "verify:polygon": "hardhat verify --network polygon"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.8.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.1"
  }
}

